import React, { Component } from "react";

class YouWelcome extends Component {
  render() {
    return (
      <div>
        <h2>We are coming from a class Component {this.props.name}</h2>
      </div>
    );
  }
}

export default YouWelcome;
